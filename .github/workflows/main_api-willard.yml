name: Build and deploy Node.js app to Azure Web App - api-willard

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'

      - name: npm install, build, and test
        run: |
          npm install
          npm run build --if-present

      - name: Create build artifact
        run: |
          mkdir artifact
          xcopy .\dist .\artifact\dist /s /e /i
          copy package.json .\artifact\
          copy package-lock.json .\artifact\
          # Crea el archivo comprimido dentro del directorio artifact
          tar -czf artifact/nestjs-app.tar.gz -C artifact .

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: artifact/nestjs-app.tar.gz

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: node-app

      - name: List files in current directory
        run: ls -R

      - name: Verify downloaded artifact
        run: |
          if [ -d ./node-app ]; then
            echo "Directory ./node-app exists."
          else
            echo "Directory ./node-app does not exist."
          fi

      - name: Extract build artifact
        run: |
          mkdir -p ./artifact
          # Verifica si el archivo existe antes de intentar descomprimirlo
          if [ -f ./node-app/nestjs-app.tar.gz ]; then
            tar -xzf ./node-app/nestjs-app.tar.gz -C ./artifact
          else
            echo "File ./node-app/nestjs-app.tar.gz not found"
            exit 1
          fi

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
          app-name: 'api-willard'
          slot-name: 'Production'
          package: ./artifact
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_AD1AFF332A4B48E2810D80C8F1150DDB }}
